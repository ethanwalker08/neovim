-- TODO: Write a debugging setup using nvim-dap and either telescope-dap or nvim-dap-ui.
-- This will likely involve setting up a floating window to show the debugging UI, and keymaps to control the debugger.
local config = {}
function config.setup()
end

return config
